'use client';import {useState} from 'react';import Link from 'next/link';import Image from 'next/image';import {motion,useReducedMotion} from 'framer-motion';
export const imgs={shower:'/projects/glass2.jpeg',pool:'/projects/glass6.jpeg',interior:'/projects/glass1.jpeg',detail:'/projects/glass3.jpeg',kitchen:'/projects/glass4.jpeg',stairs:'/projects/glass5.jpeg'};
export function Nav(){const[open,setOpen]=useState(false);return <header className="nav"><div className="shell navInner"><Link className="logo" href="/">APEX <small>FRAMELESS GLASS</small></Link><nav id="site-menu" className={`links ${open?'open':''}`} aria-label="Primary navigation">{[['/','Home'],['/services','Services'],['/services#projects','Projects'],['/contact','Contact']].map(([href,label])=><Link onClick={()=>setOpen(false)} className="link" href={href} key={label}>{label}</Link>)}<Link onClick={()=>setOpen(false)} className="quote" href="/contact">Request a quote</Link></nav><button className="mobileBtn" aria-label={`${open?'Close':'Open'} menu`} aria-expanded={open} aria-controls="site-menu" onClick={()=>setOpen(!open)}>{open?'×':'☰'}</button></div></header>}
export function RevealImage({src,alt,className}:{src:string;alt:string;className?:string}){const reduced=useReducedMotion();return <motion.div initial={reduced?false:{clipPath:'inset(0 0 100% 0)'}} whileInView={reduced?undefined:{clipPath:'inset(0 0 0% 0)'}} viewport={{once:true,amount:.2}} transition={{duration:.8,ease:[.22,1,.36,1]}} className={className}><Image src={src} alt={alt} width={1400} height={1000} sizes="(max-width: 900px) 100vw, 70vw"/></motion.div>}
export function Footer(){return <footer className="footer"><div className="shell footerGrid"><div className="logo">APEX <small>FRAMELESS GLASS</small></div><div><div className="eyebrow">Service areas</div><p>Newcastle · Lake Macquarie<br/>Central Coast · Hunter Valley</p></div><div><div className="eyebrow">Contact</div><p>04XX XXX XXX<br/>hello@apexframelessglass.com.au<br/>@ApexFramelessGlass</p></div></div></footer>}
