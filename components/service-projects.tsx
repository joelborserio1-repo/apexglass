'use client';import {useState} from 'react';import {ProjectGrid} from '@/components/project-grid';import type {Project} from '@/lib/projects';
export default function ServiceProjects({projects}:{projects:Project[]}){const [service,setService]=useState('');const services=[...new Set(projects.map(p=>p.service))];const shown=service?projects.filter(p=>p.service===service):projects;return <><label className="projectFilter">Filter projects by service<select value={service} onChange={e=>setService(e.target.value)}><option value="">All services</option>{services.map(x=><option key={x}>{x}</option>)}</select></label><ProjectGrid projects={shown}/></>}
