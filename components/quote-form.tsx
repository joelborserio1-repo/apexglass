'use client';import Script from 'next/script';import {useRef,useState} from 'react';
declare global{interface Window{turnstile?:{reset:(id?:string)=>void}}}
export default function QuoteForm(){const[status,setStatus]=useState('');const formRef=useRef<HTMLFormElement>(null);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Sending…');const form=new FormData(e.currentTarget);const token=String(form.get('cf-turnstile-response')||'');const body={...Object.fromEntries(form.entries()),turnstileToken:token};const r=await fetch('/api/quote',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await r.json();setStatus(r.ok?'Thanks. Your project details have been sent to Apex.':data.error||'Please check the form and try again.');if(r.ok){formRef.current?.reset();window.turnstile?.reset();}}return <><Script src="https://challenges.cloudflare.com/turnstile/v0/api.js" strategy="afterInteractive"/><form ref={formRef} onSubmit={submit} className="formGrid"><div className="field"><label htmlFor="name">Full name</label><input id="name" name="name" required/></div><div className="field"><label htmlFor="email">Email address</label><input id="email" name="email" type="email" required/></div><div className="field"><label htmlFor="phone">Phone number</label><input id="phone" name="phone" required/></div><div className="field"><label htmlFor="suburb">Suburb</label><input id="suburb" name="suburb"/></div><div className="field"><label htmlFor="service">Service required</label><select id="service" name="service" required><option value="">Select a service</option><option>Shower screen</option><option>Splashback</option><option>Pool fencing</option><option>Balustrade</option><option>Mirror</option><option>Window replacement</option><option>Commercial shopfront</option></select></div><div className="field"><label htmlFor="type">Project type</label><select id="type" name="type"><option>Residential</option><option>Commercial</option></select></div><div className="field full"><label htmlFor="description">Project description</label><textarea id="description" name="description" minLength={10} required/></div><div className="field full"><label htmlFor="measurements">Approximate measurements</label><input id="measurements" name="measurements"/></div><div className="honeypot" aria-hidden="true"><label>Website<input name="website" tabIndex={-1} autoComplete="off"/></label></div><div className="field full"><div className="cf-turnstile" data-sitekey={process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY} data-theme="light"/><button className="btn" type="submit">Send project details</button><p aria-live="polite">{status}</p></div></form></>}
