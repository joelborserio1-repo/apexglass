'use client';
import Script from 'next/script';import {useRef,useState} from 'react';
declare global{interface Window{turnstile?:{reset:(id?:string)=>void}}}
export default function QuoteForm(){const[status,setStatus]=useState('');const[sending,setSending]=useState(false);const formRef=useRef<HTMLFormElement>(null);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(sending)return;setSending(true);setStatus('Sending your project details…');try{const form=new FormData(e.currentTarget);const body={...Object.fromEntries(form.entries()),turnstileToken:String(form.get('cf-turnstile-response')||'')};const r=await fetch('/api/quote',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw new Error(data.error||'Please check the form and try again.');setStatus('Thanks — your project details have been sent. The Apex team will be in touch.');formRef.current?.reset();window.turnstile?.reset();}catch(error){setStatus(error instanceof Error?error.message:'Unable to send your request. Please try again.');}finally{setSending(false)}}return <><Script src="https://challenges.cloudflare.com/turnstile/v0/api.js" strategy="afterInteractive"/><form ref={formRef} onSubmit={submit} className="formGrid"><div className="field"><label htmlFor="name">Full name</label><input id="name" name="name" autoComplete="name" required/></div><div className="field"><label htmlFor="email">Email address</label><input id="email" name="email" type="email" autoComplete="email" required/></div><div className="field"><label htmlFor="phone">Phone number</label><input id="phone" name="phone" type="tel" autoComplete="tel" required/></div><div className="field"><label htmlFor="suburb">Project suburb</label><input id="suburb" name="suburb" autoComplete="address-level2"/></div><div className="field"><label htmlFor="service">Service required</label><select id="service" name="service" required><option value="">Select a service</option><option>Shower screen</option><option>Splashback</option><option>Pool fencing</option><option>Balustrade</option><option>Mirror</option><option>Window replacement</option><option>Commercial shopfront</option></select></div><div className="field"><label htmlFor="type">Project type</label><select id="type" name="type"><option>Residential</option><option>Commercial</option></select></div><div className="field full"><label htmlFor="description">Tell us about the project</label><textarea id="description" name="description" minLength={10} placeholder="What would you like installed, and when are you hoping to start?" required/></div><div className="field full"><label htmlFor="measurements">Approximate measurements <span>(optional)</span></label><input id="measurements" name="measurements" placeholder="Width × height, if known"/></div><div className="honeypot" aria-hidden="true"><label>Website<input name="website" tabIndex={-1} autoComplete="off"/></label></div><div className="field full submitField"><div className="cf-turnstile" data-sitekey={process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY} data-theme="light"/><button className="btn" type="submit" disabled={sending}>{sending?'Sending…':'Send project details'}</button><p className="formStatus" aria-live="polite">{status}</p><small>By submitting, you agree that Apex may contact you about this enquiry.</small></div></form></>}
