import Link from "next/link";import {RevealImage} from "@/components/site";import type {Project} from "@/lib/projects";
export function ProjectGrid({projects}:{projects:Project[]}){if(!projects.length)return <p className="copy emptyProjects">Portfolio projects are being prepared. Please check back soon.</p>;return <div className="projects">{projects.map((p,index)=><article className={`project ${index%2?"offset":""}`} key={p.id}><Link href={`/projects/${p.slug}`}><RevealImage src={p.images[0]?.public_url||"/projects/glass1.jpeg"} alt={p.images[0]?.alt_text||p.title}/><div className="projectCaption"><h3>{p.title}</h3><span className="muted">{p.service} · {p.location}{p.year?` · ${p.year}`:""}</span></div></Link></article>)}</div>}
