'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {usePathname} from 'next/navigation';
const links=[['/','Home'],['/services','Services'],['/services#projects','Projects'],['/contact','Contact']] as const;
export function Nav(){const[open,setOpen]=useState(false);const pathname=usePathname();useEffect(()=>setOpen(false),[pathname]);return <header className="nav"><div className="shell navInner"><Link className="logo" href="/" aria-label="Apex Frameless Glass home">APEX <small>FRAMELESS GLASS</small></Link><nav id="site-menu" className={`links ${open?'open':''}`} aria-label="Primary navigation">{links.map(([href,label])=><Link className="link" href={href} key={label} aria-current={href==='/'?pathname==='/'?'page':undefined:pathname.startsWith(href.split('#')[0])?'page':undefined}>{label}</Link>)}<Link className="quote" href="/contact">Request a quote</Link></nav><button className="mobileBtn" type="button" aria-label={`${open?'Close':'Open'} menu`} aria-expanded={open} aria-controls="site-menu" onClick={()=>setOpen(value=>!value)}>{open?'Close':'Menu'}</button></div></header>}
