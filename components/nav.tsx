'use client';
import {useEffect,useState} from 'react';import Image from 'next/image';import Link from 'next/link';import {usePathname} from 'next/navigation';
const links=[['/','Home'],['/services','Services'],['/#projects','Projects'],['/contact','Contact']] as const;
export function Nav(){const[open,setOpen]=useState(false);const pathname=usePathname();useEffect(()=>setOpen(false),[pathname]);return <header className="nav"><div className="shell navInner"><Link className="brandLogoLink" href="/" aria-label="Apex Frameless Glass home"><Image className="brandLogo brandLogoHeader" src="/apex-frameless-glass-logo.png" alt="Apex Frameless Glass" width={1137} height={1142} priority/></Link><nav id="site-menu" className={`links ${open?'open':''}`} aria-label="Primary navigation">{links.map(([href,label])=><Link className="link" href={href} key={label} aria-current={!href.includes('#')&&(href==='/'?pathname==='/' : pathname.startsWith(href))?'page':undefined}>{label}</Link>)}<Link className="quote" href="/contact">Request a quote</Link></nav><button className="mobileBtn" type="button" aria-label={`${open?'Close':'Open'} menu`} aria-expanded={open} aria-controls="site-menu" onClick={()=>setOpen(value=>!value)}>{open?'Close':'Menu'}</button></div></header>}
